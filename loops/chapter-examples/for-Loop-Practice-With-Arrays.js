// create an array variable containing the names
let doherty = ["Cypress", "Jay", "Lisa", "Gene", "Allyson", "Patty", "Primrose", "Finley"];
// write a for loop that prints each name on a different line
for (i = 0; i < doherty.length; i++){
    console.log(doherty[i]);
}